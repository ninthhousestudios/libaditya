move panchanga boxes square to sides, adjust size, adjust text placement

for any transit with a placename, have chtk2sbc write that to a complete list of
transit placenames and information; i.e., build a database of places from the
files converted

add command line options for defaults and transits to sbc and chtk2sbc

have a default output directory just like there is a default input file
directory; if there is a slash in the input/output file directory name, treat it
is a relative, then absolute path

make an option where the output file has each option field setting indicated in
the output file name


check out the timing on this, the change and name of karana
pyphemeris -p lara-hlopec.chtk -a 27 -d 08/15/1992 -t 02:02:00
